# Сводка исправлений

## Исправление 1: Построение дуги через 3 точки

**Файлы:**
- `core/MathUtils.h` - добавлена функция `getCircleCenterFrom2PointsAndRadius`
- `core/Tools.cpp` - исправлена логика построения дуги (метод 1)

**Изменения:**
- Радиус определяется как расстояние от первой точки до третьей точки
- Центр окружности находится через функцию `getCircleCenterFrom2PointsAndRadius`
- Дуга ограничена первой (начало) и второй (конец) точками

**Строки кода:**
- `core/Tools.cpp:305-356` - логика построения дуги
- `core/Tools.cpp:426-481` - отрисовка предварительной дуги

## Исправление 2: Выбор объекта кликом

**Файлы:**
- `ui/windows/Viewport.h` - добавлена функция `pickObjectAtPoint`
- `ui/windows/Viewport.cpp` - исправлена логика выбора объекта

**Изменения:**
- Добавлена функция `pickObjectAtPoint` для поиска объекта по точке клика
- Порог попадания уменьшен до 3 пикселей
- Объект выбирается только если клик попал непосредственно по нему
- Если клик не попал по объекту, активируется выделение рамкой

**Строки кода:**
- `ui/windows/Viewport.cpp:191-212` - обработка клика мыши
- `ui/windows/Viewport.cpp:251-282` - функция `pickObjectAtPoint`

## Важно для пересборки проекта:

1. Убедитесь, что все файлы сохранены
2. Очистите предыдущую сборку: `rm -rf build/`
3. Пересоберите проект: `cmake -B build && cmake --build build`

